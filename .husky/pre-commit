#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Example pre-commit checks (you can customize)
echo "✅ Husky pre-commit running..."

# Run lint before committing
npm run lint

# Run type check
npm run type-check

# Run tests (optional, can be skipped for speed)
npm test
