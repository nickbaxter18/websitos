#!/bin/sh

echo "✅ Husky pre-commit running..."

# Run lint-staged (prettier + eslint on staged files)
pnpm lint-staged --config package.json || true

# Optionally run type check and tests (uncomment if needed)
# pnpm type-check
# pnpm test

exit 0