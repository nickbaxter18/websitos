#!/bin/sh

. "$(dirname "$0")/_/husky.sh"

echo "✅ Husky pre-commit running..."

# Run lint-staged (Prettier, ESLint, Black on staged files only)
npx lint-staged --config package.json || true

exit 0