# Codecov configuration
coverage:
  precision: 2
  round: down
  range: "50...90"

  status:
    project:
      default:
        target: auto
        threshold: 2
    patch:
      default:
        target: auto
        threshold: 2

ignore:
  - "migrations/**"
  - "**/*.d.ts"
  - "**/*.test.*"
  - "**/*.stories.*"
