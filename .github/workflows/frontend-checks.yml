name: Frontend Checks

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [lint, prettier, type-check, test-unit, playwright-e2e]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Cache pnpm store
        uses: actions/cache@v3
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        if: ${{ matrix.task == 'lint' }}
        run: |
          (pnpm exec eslint src v2 ops --format json --output-file eslint-output.json) || true
          echo $? > exit-code.txt
          node Scripts/convert-eslint.cjs eslint-output.json eslint-report.json exit-code.txt || echo '{"workflow":"frontend-checks","job":"lint","status":"failure","errors":[{"message":"ESLint converter failed"}],"warnings":[],"notices":[]}' > eslint-report.json
          mkdir -p summaries
          mv eslint-report.json summaries/
          mv eslint-output.json summaries/frontend-lint-raw.log || true
          mv exit-code.txt summaries/frontend-lint-exit-code.txt || true
          sha256sum summaries/* > summaries/checksums.txt || true

      - name: Run Prettier
        if: ${{ matrix.task == 'prettier' }}
        run: |
          (pnpm exec prettier --check . > prettier-output.txt 2>&1) || true
          echo $? > exit-code.txt
          node Scripts/convert-prettier.cjs prettier-output.txt prettier-report.json exit-code.txt || echo '{"workflow":"frontend-checks","job":"prettier","status":"failure","errors":[{"message":"Prettier converter failed"}],"warnings":[],"notices":[]}' > prettier-report.json
          mkdir -p summaries
          mv prettier-report.json summaries/
          mv prettier-output.txt summaries/frontend-prettier-raw.log || true
          mv exit-code.txt summaries/frontend-prettier-exit-code.txt || true
          sha256sum summaries/* > summaries/checksums.txt || true

      - name: Run TypeScript type-check
        if: ${{ matrix.task == 'type-check' }}
        run: |
          (pnpm exec tsc -p tsconfig.ci.json --noEmit --skipLibCheck > tsc-output.txt 2>&1) || true
          echo $? > exit-code.txt
          node Scripts/convert-tsc.cjs tsc-output.txt tsc-report.json exit-code.txt || echo '{"workflow":"frontend-checks","job":"tsc","status":"failure","errors":[{"message":"TSC converter failed"}],"warnings":[],"notices":[]}' > tsc-report.json
          mkdir -p summaries
          mv tsc-report.json summaries/
          mv tsc-output.txt summaries/frontend-tsc-raw.log || true
          mv exit-code.txt summaries/frontend-tsc-exit-code.txt || true
          sha256sum summaries/* > summaries/checksums.txt || true

      - name: Run Jest unit tests
        if: ${{ matrix.task == 'test-unit' }}
        run: |
          (pnpm exec jest --coverage --json --outputFile=jest-output.json) || true
          echo $? > exit-code.txt
          node Scripts/convert-jest.cjs jest-output.json jest-report.json exit-code.txt || echo '{"workflow":"frontend-checks","job":"jest-unit","status":"failure","errors":[{"message":"Jest converter failed"}],"warnings":[],"notices":[]}' > jest-report.json
          mkdir -p summaries
          mv jest-report.json summaries/
          mv jest-output.json summaries/frontend-jest-raw.log || true
          mv exit-code.txt summaries/frontend-jest-exit-code.txt || true
          sha256sum summaries/* > summaries/checksums.txt || true

      - name: Run Playwright E2E tests
        if: ${{ matrix.task == 'playwright-e2e' }}
        run: |
          pnpm exec playwright install --with-deps || true
          (pnpm exec playwright test 2>&1 | tee playwright-output.txt) || true
          echo $? > exit-code.txt
          (npx nyc report --reporter=json-summary --report-dir=coverage/e2e) || true

          node Scripts/convert-playwright.cjs playwright-output.txt coverage/e2e/coverage-summary.json playwright-e2e-report.json exit-code.txt || true

          if [ ! -s playwright-e2e-report.json ]; then
            echo '{"workflow":"frontend-checks","job":"playwright-e2e","status":"failure","errors":[{"message":"Converter crashed or no output"}],"warnings":[],"notices":[]}' > playwright-e2e-report.json
          fi

          mkdir -p summaries
          mv playwright-e2e-report.json summaries/ || true
          mv coverage/e2e/coverage-summary.json summaries/frontend-playwright-coverage-raw.json || true

          mv playwright-output.txt summaries/frontend-playwright-raw.log || true
          mv exit-code.txt summaries/frontend-playwright-exit-code.txt || true

          sha256sum summaries/* > summaries/checksums.txt || true

      - name: Generate Artifact Manifest
        run: |
          echo "📦 Artifact manifest for frontend-${{ matrix.task }}" > summaries/artifact-manifest.txt
          find summaries -type f -printf "%p (%s bytes)\n" >> summaries/artifact-manifest.txt

      - name: Upload frontend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-partial-${{ matrix.task }}
          path: summaries/
          retention-days: 2

      - name: Upload raw Playwright artifacts
        if: ${{ matrix.task == 'playwright-e2e' }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-e2e-artifacts
          path: test-results/
          retention-days: 2

  package-and-upload:
    runs-on: ubuntu-latest
    needs: checks
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all frontend job artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./downloaded

      - name: Collect frontend summaries
        run: |
          mkdir -p summaries
          echo "📥 Checking downloaded artifacts:" > summaries/artifact-manifest.txt
          find downloaded -type f -printf "%p (%s bytes)\n" >> summaries/artifact-manifest.txt

          for f in $(find downloaded -type f -iname "*-report.json"); do
            echo "📥 Found report: $f"
            cp "$f" summaries/
          done

          PLAYWRIGHT_FILE=$(find downloaded -type f -iname "playwright-e2e-report.json" | head -n 1)
          if [ -n "$PLAYWRIGHT_FILE" ]; then
            echo "📥 Using real Playwright report: $PLAYWRIGHT_FILE"
            cp "$PLAYWRIGHT_FILE" summaries/playwright-e2e-report.json
          else
            echo "⚠️ No playwright-e2e-report.json found in artifacts, injecting dummy"
            echo '{"workflow":"frontend-checks","job":"playwright-e2e","status":"neutral","errors":[],"warnings":[],"notices":[{"message":"playwright-e2e-report.json missing in aggregator — dummy injected"}]}' > summaries/playwright-e2e-report.json
          fi

          for required in playwright-e2e-report.json eslint-report.json prettier-report.json tsc-report.json jest-report.json; do
            if [ ! -f summaries/$required ]; then
              echo "⚠️ Missing $required in summaries/"
              echo '{"workflow":"frontend-checks","job":"missing","status":"neutral","errors":[],"warnings":[],"notices":[{"message":"'$required' missing from artifact collection"}]}' > summaries/$required
            fi
          done

          echo "📂 Collected summaries (with sizes):"
          ls -lah summaries || true

          sha256sum summaries/* > summaries/checksums.txt

      - name: Upload combined frontend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-summaries
          path: summaries/
          retention-days: 2