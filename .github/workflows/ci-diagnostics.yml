name: CI Diagnostics

on:
  workflow_call:
    secrets:
      LINEAR_API_KEY:
        required: true
      LINEAR_TEAM_ID:
        required: true
  workflow_dispatch:

permissions:
  checks: write
  contents: read
  actions: read

jobs:
  diagnostics:
    name: Run CI Diagnostics
    runs-on: ubuntu-latest
    if: always()
    env:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
      LINEAR_TEAM_ID: ${{ secrets.LINEAR_TEAM_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create pending CI Diagnostics check
        uses: actions/github-script@v7
        with:
          script: |
            const { data: check } = await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: "CI Diagnostics",
              head_sha: process.env.GITHUB_SHA,
              status: "in_progress",
              output: {
                title: "CI Diagnostics Running",
                summary: "Diagnostics workflow has started."
              }
            });
            core.setOutput("check_id", check.id);

      - name: Run diagnostics queries
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            if (!process.env.LINEAR_API_KEY || !process.env.LINEAR_TEAM_ID) {
              console.log("❌ Missing Linear secrets");
              fs.writeFileSync('report.md', `❌ Linear API key or team ID not configured.`);
              return;
            }

            const token = process.env.LINEAR_API_KEY.trim();

            async function runQuery(query, variables = {}) {
              const res = await fetch("https://api.linear.app/graphql", {
                method: "POST",
                headers: {
                  "Content-Type": "application/json",
                  "Authorization": `Bearer ${token}`
                },
                body: JSON.stringify({ query, variables })
              });
              return res.json();
            }

            let report = "## ⚠️ CI Diagnostics Report\n";

            // Viewer check
            const viewer = await runQuery("query { viewer { id name email organization { id name } } }");
            if (viewer.errors) {
              report += `\n❌ Viewer query error: ${JSON.stringify(viewer.errors)}`;
            } else {
              report += `\n✅ Viewer: ${JSON.stringify(viewer.data)}`;
            }

            // Teams check
            const teams = await runQuery("query { teams { nodes { id name } } }");
            if (teams.errors) {
              report += `\n❌ Teams query error: ${JSON.stringify(teams.errors)}`;
            } else {
              const team = teams.data.teams.nodes.find(t => t.id === process.env.LINEAR_TEAM_ID.trim());
              if (team) {
                report += `\n✅ Team ID valid: ${team.id} (${team.name})`;
              } else {
                report += `\n❌ LINEAR_TEAM_ID does not match any team. Found: ${JSON.stringify(teams.data.teams.nodes)}`;
              }
            }

            fs.writeFileSync('report.md', report);

      - name: Sync diagnostics to Linear
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const token = process.env.LINEAR_API_KEY.trim();
            const teamId = process.env.LINEAR_TEAM_ID.trim();
            const commit = process.env.GITHUB_SHA;
            const branch = process.env.GITHUB_REF_NAME;

            const description = fs.readFileSync('report.md', 'utf8');
            const title = `CI Diagnostics (${branch}, ${commit.substring(0,7)})`;

            async function runQuery(query, variables = {}) {
              const res = await fetch("https://api.linear.app/graphql", {
                method: "POST",
                headers: {
                  "Content-Type": "application/json",
                  "Authorization": `Bearer ${token}`
                },
                body: JSON.stringify({ query, variables })
              });
              return res.json();
            }

            // Check if issue exists
            const searchQuery = `query Issues($filter: IssueFilter) { issues(filter: $filter, first: 1) { nodes { id url title } } }`;
            const searchVars = { filter: { title: { eq: title } } };
            const existing = await runQuery(searchQuery, searchVars);

            if (existing.data?.issues?.nodes?.length) {
              // Update existing issue
              const issueId = existing.data.issues.nodes[0].id;
              const updateMutation = `mutation IssueUpdate($id: String!, $input: IssueUpdateInput!) { issueUpdate(id: $id, input: $input) { success issue { id url } } }`;
              const updateVars = { id: issueId, input: { description, teamId } };
              const updated = await runQuery(updateMutation, updateVars);
              console.log("🔄 Updated Linear issue:", updated);
              fs.appendFileSync('report.md', `\n\n🔄 Updated Linear issue: ${updated?.data?.issueUpdate?.issue?.url || 'n/a'}`);
            } else {
              // Create new issue
              const createMutation = `mutation IssueCreate($input: IssueCreateInput!) { issueCreate(input: $input) { success issue { id url } } }`;
              const createVars = { input: { title, description, teamId } };
              const created = await runQuery(createMutation, createVars);
              console.log("🆕 Created Linear issue:", created);
              fs.appendFileSync('report.md', `\n\n🆕 Created Linear issue: ${created?.data?.issueCreate?.issue?.url || 'n/a'}`);
            }

      - name: Complete CI Diagnostics check
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const summary = fs.readFileSync('report.md', 'utf8').slice(0, 60000);

            await github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: "CI Diagnostics",
              head_sha: process.env.GITHUB_SHA,
              status: "completed",
              conclusion: "success",
              output: {
                title: "CI Diagnostics Report",
                summary
              }
            });