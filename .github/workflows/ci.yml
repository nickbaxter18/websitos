name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  checks: write
  contents: read
  actions: read

jobs:
  # ---------------------------
  # Frontend Jobs
  # ---------------------------
  frontend-checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [lint, prettier, type-check, test-unit, playwright-e2e]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: pnpm/action-setup@v2
        with:
          version: 10
      - uses: actions/cache@v3
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
      - run: pnpm install --frozen-lockfile

      # (frontend job steps unchanged)

      - name: Upload partial frontend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-partial-${{ matrix.task }}
          path: summaries/
          retention-days: 2

  package-frontend:
    runs-on: ubuntu-latest
    needs: frontend-checks
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Download all frontend job artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: frontend-partial-*
          path: ./downloaded
          merge-multiple: true

      - name: Debug downloaded artifacts
        run: |
          echo "📂 Downloaded artifacts for frontend packaging:"
          find downloaded || true

      - name: Collect frontend summaries
        run: |
          mkdir -p summaries
          find downloaded -type f -iname "*-report.json" -exec cp {} summaries/ \; || true
          find downloaded -type f -iname "artifact-manifest.txt" -exec cp {} summaries/ \; || true
          sha256sum summaries/* > summaries/checksums.txt || true
          echo "📦 Packaged frontend summaries:" > summaries/artifact-manifest.txt
          find summaries -type f -printf "%p (%s bytes)\n" >> summaries/artifact-manifest.txt

      - name: Upload combined frontend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-summaries
          path: summaries/
          retention-days: 2

  # ---------------------------
  # Backend Jobs
  # ---------------------------
  backend-checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [black, mypy, pytest, flake8]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest-json-report flake8 mypy black

      # (backend job steps unchanged)

      - name: Upload partial backend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-partial-${{ matrix.task }}
          path: summaries/
          retention-days: 2

  package-backend:
    runs-on: ubuntu-latest
    needs: backend-checks
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Download all backend job artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: backend-partial-*
          path: ./downloaded
          merge-multiple: true

      - name: Debug downloaded artifacts
        run: |
          echo "📂 Downloaded artifacts for backend packaging:"
          find downloaded || true

      - name: Collect backend summaries
        run: |
          mkdir -p summaries
          find downloaded -type f -iname "*-report.json" -exec cp {} summaries/ \; || true
          find downloaded -type f -iname "artifact-manifest.txt" -exec cp {} summaries/ \; || true
          sha256sum summaries/* > summaries/checksums.txt || true
          echo "📦 Packaged backend summaries:" > summaries/artifact-manifest.txt
          find summaries -type f -printf "%p (%s bytes)\n" >> summaries/artifact-manifest.txt

      - name: Upload combined backend summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-summaries
          path: summaries/
          retention-days: 2

  # ---------------------------
  # Coverage Jobs
  # ---------------------------
  coverage-checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task: [frontend, backend, backend-node, e2e]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        if: ${{ matrix.task != 'backend' }}
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
      - name: Setup pnpm
        if: ${{ matrix.task == 'frontend' || matrix.task == 'e2e' || matrix.task == 'backend-node' }}
        uses: pnpm/action-setup@v2
        with:
          version: 10
      - name: Setup Python
        if: ${{ matrix.task == 'backend' }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # (coverage job steps unchanged)

      - name: Upload partial coverage summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-partial-${{ matrix.task }}
          path: summaries/
          retention-days: 2

  package-coverage:
    runs-on: ubuntu-latest
    needs: coverage-checks
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Download all coverage job artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-partial-*
          path: ./downloaded
          merge-multiple: true

      - name: Debug downloaded artifacts
        run: |
          echo "📂 Downloaded artifacts for coverage packaging:"
          find downloaded || true

      - name: Collect coverage summaries
        run: |
          mkdir -p summaries
          find downloaded -type f -iname "*-report.json" -exec cp {} summaries/ \; || true
          find downloaded -type f -iname "artifact-manifest.txt" -exec cp {} summaries/ \; || true
          sha256sum summaries/* > summaries/checksums.txt || true
          echo "📦 Packaged coverage summaries:" > summaries/artifact-manifest.txt
          find summaries -type f -printf "%p (%s bytes)\n" >> summaries/artifact-manifest.txt

      - name: Upload combined coverage summaries
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-summaries
          path: summaries/
          retention-days: 2

  # ---------------------------
  # Central Diagnostics
  # ---------------------------
  diagnostics:
    if: always()
    needs: [package-frontend, package-backend, package-coverage]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download frontend partials
        uses: actions/download-artifact@v4
        with:
          pattern: frontend-partial-*
          path: ./summaries/frontend
          merge-multiple: true

      - name: Download backend partials
        uses: actions/download-artifact@v4
        with:
          pattern: backend-partial-*
          path: ./summaries/backend
          merge-multiple: true

      - name: Download coverage partials
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-partial-*
          path: ./summaries/coverage
          merge-multiple: true

      - name: Debug downloaded artifacts
        run: |
          echo "📂 Listing summaries directory contents"
          find summaries -type f -maxdepth 3 || true

      - name: Run Diagnostics Workflow
        uses: ./.github/workflows/ci-diagnostics.yml